package lexer;

public enum TokenType {

    // identifiers
    NUMBER, STRING, IDENTIFIER, WHITESPACE, ENDLINE,

    // keywords
    LET, SHOW,

    // operaters
    ADD, SUBTRACT, MULTIPLY, DIVIDE, ASSIGN

}
